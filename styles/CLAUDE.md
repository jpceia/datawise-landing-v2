# Styles

## globals.css
- Tailwind directives (@tailwind base, components, utilities)
- Smooth scrolling (`scroll-behavior: smooth`)
- Base heading styles (font-bold on all headings)
- Container component (`.container` with max-w-7xl)
- Globe wrapper styles (`.globe-wrapper`)
- Custom animation (`.animate-fadeIn`)

## Tailwind Config (`/tailwind.config.js`)
- **Colors:** primary (blue #0D47A1), secondary (gray-blue #455A64), success, warning, error, gray scale
- **Font:** Inter sans-serif
- **Custom spacing:** half-step values (4.5, 5.5, etc.)
- **Animations:** blob (floating blobs), bounce
- **Screens:** xs (475px), 3xl (1920px)
